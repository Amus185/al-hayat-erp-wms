{
  "name": "al-hayat-erp-wms",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "workers-api",
    "web",
    "mobile"
  ],
  "scripts": {
    "dev:backend": "npm --workspace workers-api run dev",
    "dev:web": "npm --workspace web run dev",
    "dev:mobile": "npm --workspace mobile run start",
    "build": "npm --workspace web run build"
  }
}

